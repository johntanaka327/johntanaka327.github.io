# johntanaka327.github.io
github io repositoy

In index.html, we just redirect to `${host_name}/portfolio`.

That endpoint is made by "gh-pages" from "portfolio" repositoy.
That project is based by create-react-app.
